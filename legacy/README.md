# Legacy 旧版适配器

本目录保留项目早期供小爱课程表官方适配器环境使用的三个脚本：

- `provider.js`：通过同步 XHR 读取教务课表数据。
- `parser.js`：转换课程字段，并包含过滤“大学英语”的历史逻辑。
- `timer.js`：通过同步 XHR 读取学期与节次配置。

官方适配器登录链路已经失效，这些文件自 v1.0.0 起停止维护，仅用于追溯旧实现。当前产品和安装入口是仓库根目录的 `xiaoai-schedule-cdu.user.js`。
